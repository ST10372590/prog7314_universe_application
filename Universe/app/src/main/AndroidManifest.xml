<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/unilogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/unilogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Universe"
        android:networkSecurityConfig="@xml/network_security_config">

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".LecturerDashboardActivity" />
        <activity android:name=".CourseDetailActivity" />
        <activity android:name=".UploadAssessmentActivity" />
        <activity android:name=".ModuleTeachMenuActivity" />
        <activity android:name=".FileSharingActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".GamificationActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".DirectMessageActivity" />
        <activity android:name=".CommunicationHubActivity" />
        <activity android:name=".StudentEnrolledCoursesActivity" />
        <activity android:name=".CourseModulesActivity" />
        <activity android:name=".ModuleContentActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".StudentAssessmentActivity" />
        <activity android:name=".SubmitAssessmentActivity" />
        <activity android:name=".StudentSubmissionsActivity" />
        <activity android:name=".SubmissionDetailsActivity" />
        <activity android:name=".LecturerSubmissionReviewActivity" />
        <activity android:name=".LecturerSubmissionsActivity" />

    </application>

</manifest>
